# 1. Initialize the repo (if not already active)
git init

# 2. Secure the remote target (Your Repo)
# If it says "remote exists", ignore the error.
git remote add origin https://github.com/m5trevino/trevino_war_room.git

# 3. Stage the entire armory (All 12 files + new logic)
git add .

# 4. Commit the package with a Timestamp
git commit -m "WAR ROOM v6.0: FULL DEPLOYMENT CLUSTER (PDF ENGINE + INDEED UPLINK)"

# 5. EXECUTE THE PUSH (Force overwrite to ensure your local version is King)
git push -u origin main --force
⚡ DROP
