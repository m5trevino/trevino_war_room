# 1. Initialize (if you haven't already, though you likely have)
git init

# 2. Add Remote (Ensure this matches your actual repo URL)
git remote add origin https://github.com/m5trevino/trevino_war_room.git
# (If it says "remote origin already exists", ignore this step)

# 3. Stage All 12 Updated Files
git add .

# 4. Commit with a Timestamp
git commit -m "WAR ROOM v5.5: DEPLOYMENT MODE ACTIVATED (PDF/DIR/JOB CLUSTER)"

# 5. Push to Main (Force if necessary to align history)
git push -u origin main
⚡ DROP
